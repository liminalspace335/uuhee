-- ============================================================
-- 유유히 — 공간(branches) 설명 컬럼 추가
-- Supabase 대시보드 → SQL Editor → New query → 전체 붙여넣고 Run
-- (어드민 "공간" 탭이 폴더 방식에서 지점 연동 방식으로 바뀌면서
--  지점별 공간 설명을 저장할 컬럼이 필요합니다)
-- ============================================================

alter table branches add column if not exists space_desc text;
